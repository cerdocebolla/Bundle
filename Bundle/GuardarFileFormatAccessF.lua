local function = askForFile
    when file.custom setSettings = true
    then askForAdvance 
        let function stop.Function 
            goUp.applySwap = true 
            function.short = enum.KeyCode = "F" 
                when F = true (setAsFunction) then goDown
                    when downApply.swap.createChAIN
        nameParent = chain.true addLast = force.True.force 
        then create parentName.logIn.Insert = Chain 
            inBracket "Chain" if alternative downApply.swap.runRight
            chain.create.applySwap.sewageApp.productChild.true then
                let function endInstant.force = true
                then endInstant.endInstant 
                end)
            end)
        end)
    end)
end) 
}

-- Scripted by Cerdocebolla's Software. All rights reserved.